name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      # The build downscales the toolbar icons with sharp, so the dev
      # dependencies have to be there before anything below runs.
      - name: Install dependencies
        run: npm ci

      # Matches the signature rules against the real bundle output committed
      # under test/fixtures.
      - name: Signature tests
        run: npm test

      - name: Build both browser targets
        run: npm run build

      # Catches a manifest that points at a file the build did not copy, or the
      # two manifests drifting apart.
      - name: Validate build output
        run: npm run validate
